# Sorted squares
class Solution:
    def sortedSquares(self, nums: List[int]) -> List[int]:
        result = []
        for num in nums:
          number = num
          number **= 2
          result.append(number)
        result.sort()
        return(result)



result = [num ** 2 for num in nums]
result.sort()
return(result)
